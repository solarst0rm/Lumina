"""项目统一配置文件"""
import os

# API配置（从环境变量加载）
API_KEY = os.environ.get("xxx_KEY", "")
if not API_KEY:
    print("警告: 环境变量 xxx_KEY 未设置，AI功能将无法使用")
BASE_URL = "https://api-inference.modelscope.cn/v1"
MODEL_NAME = "Qwen/Qwen3-VL-8B-Instruct"

# 路径配置
UPLOAD_FOLDER = 'uploads'
DEFAULT_SUMMARY_FILENAME = "summary.md"
DEFAULT_EXERCISE_FILENAME = "exercise.md"
DEFAULT_EXERCISE_JSON_FILENAME = "exercise.json"

# 限制配置
MAX_CONTENT_LENGTH = 16 * 1024 * 1024  # 16MB
ALLOWED_EXTENSIONS = {'pdf', 'jpg', 'jpeg', 'png', 'pptx'}
MAX_IMAGE_SIDE = 768  # 图片最大边长
PROCESS_TIMEOUT = 300  # 处理超时时间（5分钟）

# 盲生适配默认提示词
DEFAULT_USER_PROMPT = "请对文档进行全面总结，提取关键知识点"
FINAL_PROMPT_TEMPLATE = """{user_prompt}。下面是一份由多页图片组成的文档。
请你综合所有页面的内容，理解整体逻辑与结构。
请输出一个体系化的总结，使用 Markdown 标题层级（#、##、###）组织内容。
文档应直接从正文开始，不要生成任何形式的目录、内容提要或章节索引。
不要在正文中出现“目录”“如下所示”“本节包括”“文档总结”等描述结构的段落。
内容应是对整个文档的整体总结，而不是按页或按图逐条罗列。
【核心要求：适配盲生阅读，禁止出现任何公式和特殊符号】：
1. 所有数学公式（如LaTeX格式、算式符号）必须完全转换为中文自然语言描述，例如：
   - 将 E=mc^2 改为“能量等于质量乘以光速的平方”；
   - 将 2^n 改为“2的n次方”；
   - 将 sum(i=1 to n)x_i 改为“从i等于1到n的所有x_i的和”；
   - 将分数 1/2 改为“二分之一”，百分比 50% 改为“百分之五十”；
2. 所有特殊符号（包括但不限于希腊字母、数学符号、单位符号、标点类特殊符号）必须转换为中文名称，例如：
   - 将 α 改为“阿尔法”，β 改为“贝塔”，γ 改为“伽马”；
   - 将 × 改为“乘”，÷ 改为“除以”，≥ 改为“大于等于”，≤ 改为“小于等于”；
   - 将 ℃ 改为“摄氏度”，m/s 改为“米每秒”，kg 改为“千克”；
3. 禁止使用任何符号式缩写（如 &、@、#、* 等），全部替换为中文，例如：& 改为“和”，@ 改为“在”；
4. 表格内容需用纯中文自然语言描述（禁止使用Markdown表格格式），例如表格有2行3列，第一行是标题“姓名、年龄”，第二行是“张三、20岁”，需描述为“表格包含姓名和年龄两列，其中记录了张三的年龄为20岁”；
5. 图像内容用详细的中文自然语言解释其含义、结构、关键信息，确保盲生能通过文字理解图像内容；
严格保留内容的语义完整性，转换后的表述必须准确无误，不要遗漏任何信息，只输出 Markdown 正文内容，不要添加任何额外说明。
6. 输出的内容请控制行宽，每行不超过80个中文字符，段落之间保留空行，标题层级清晰，便于读屏软件逐行朗读。"""

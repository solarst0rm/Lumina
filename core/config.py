"""Project configuration."""

from __future__ import annotations

import os


API_KEY = os.environ.get("xxx_KEY", "").strip()
if not API_KEY:
    print("Warning: environment variable xxx_KEY is not set; AI features will be unavailable.")

BASE_URL = os.environ.get("BASE_URL", "https://api-inference.modelscope.cn/v1").rstrip("/")
MODEL_NAME = os.environ.get("MODEL_NAME", "Qwen/Qwen3-VL-8B-Instruct")

UPLOAD_FOLDER = "uploads"
DEFAULT_SUMMARY_FILENAME = "summary.md"
DEFAULT_EXERCISE_FILENAME = "exercise.md"
DEFAULT_EXERCISE_JSON_FILENAME = "exercise.json"

MAX_CONTENT_LENGTH = 16 * 1024 * 1024
ALLOWED_EXTENSIONS = {
    "pdf",
    "docx",
    "pptx",
    "pptm",
    "ppsx",
    "ppsm",
    "potx",
    "potm",
    "jpg",
    "jpeg",
    "png",
    "bmp",
    "webp",
    "gif",
    "tif",
    "tiff",
}
MAX_IMAGE_SIDE = 768
PROCESS_TIMEOUT = 300
SECURITY_SENSITIVE_KEYWORDS = {
    "attacklab",
    "attack lab",
    "buffer overflow",
    "stack overflow",
    "return-oriented programming",
    "rop",
    "shellcode",
    "exploit",
    "payload",
    "gadget",
    "overflow",
    "format string",
    "ret2libc",
    "code injection",
    "ctf",
    "pwn",
}

DEFAULT_USER_PROMPT = "请用简体中文对上传资料进行结构清晰、重点明确、适合读屏的总结。"

FINAL_PROMPT_TEMPLATE = """{user_prompt}

你将收到由文档页面转成的一张或多张图片，请用简体中文输出完整总结。

要求：
- 下面是一份由多页图片组成的文档。
请你综合所有页面的内容，理解整体逻辑与结构。
请输出一个体系化的总结，使用 Markdown 标题层级（#、##、###）组织内容。
文档应直接从正文开始，不要生成任何形式的目录、内容提要或章节索引。
不要在正文中出现"目录""如下所示""本节包括""文档总结"等描述结构的段落。
内容应是对整个文档的整体总结，而不是按页或按图逐条罗列。

【最重要的前提：本文档的读者是视障（盲人）学生】
这些学生完全依赖读屏软件听读你的输出内容，无法看到任何视觉信息。
你必须站在"用文字替代眼睛"的角度来写作，让读者仅通过听觉就能完整理解文档的全部含义。

【视觉内容的无障碍转写——这是核心要求】：
1. 颜色描述：文档中出现的所有颜色信息都必须保留并用文字详细说明其含义和用途。
   例如"红色标注的重点内容"应转写为"以下是原文用红色标注的重点内容，红色在此表示需要特别关注"；
   涉及色彩理论时（如三原色、冷暖色调），需用文字解释每种颜色给人的感觉和象征意义，
   例如"红色通常传达热情、激烈的感觉，蓝色通常传达冷静、安宁的感觉"；
2. 图片和插图：必须用详尽的中文自然语言描述图片中的所有视觉元素，包括：
   - 画面构图（前景、中景、背景各有什么）；
   - 人物的姿态、表情、动作、服饰特征；
   - 场景的空间关系（上下左右、远近大小）；
   - 光影效果、色调氛围（如"画面整体偏暖色调，光线从左上方照入"）；
   - 该图在文档中的作用和要表达的含义；
3. 图表和数据可视化：折线图、柱状图、饼图等必须用文字完整描述数据趋势和关键数值，
   例如"柱状图显示从2020年到2023年，销售额逐年增长，
   其中2020年为一百万元，2023年增长至三百五十万元，增幅最大的是2021到2022年"；
4. 视觉比喻和修辞：文学、艺术类文档中常出现依赖视觉经验的比喻（如"色彩斑斓""层次分明""留白"
   "泼墨""光影交错"等），必须用盲生能理解的方式解释其含义，
   例如"留白"应解释为"画面中故意不画内容的空白区域，用来给观者想象的空间，
   营造意境深远的效果"；
   "色彩斑斓"应解释为"由多种鲜艳的颜色交织在一起，给人丰富热闹的感觉"；
5. 艺术作品赏析：涉及绘画、雕塑、建筑、设计等视觉艺术内容时，必须详细描述作品的视觉特征
   并解释其艺术手法和审美价值，不能只写抽象评价（如"构图巧妙"），
   而要具体说明"画面左侧是一棵大树占据了三分之一的空间，
   右侧是开阔的田野延伸到远方，这种左重右轻的构图方式让视线自然从近处引向远方，
   产生深远辽阔的感觉"；
6. 版式和排版信息：如果文档的视觉排版本身传达了信息（如分栏对比、高亮标注、
   箭头流程等），需用文字说明这些排版的逻辑关系，
   例如"原文用左右两栏对比的方式展示了古典风格和现代风格的区别"；

【公式和符号的无障碍转写】：
1. 所有数学公式必须完全转换为中文自然语言描述，例如：
   - 将 E=mc^2 改为"能量等于质量乘以光速的平方"；
   - 将 2^n 改为"2的n次方"；
   - 将分数 1/2 改为"二分之一"，百分比 50% 改为"百分之五十"；
2. 所有特殊符号必须转换为中文名称，例如：
   - 希腊字母：α 改为"阿尔法"，β 改为"贝塔"，γ 改为"伽马"；
   - 数学符号：× 改为"乘"，÷ 改为"除以"，≥ 改为"大于等于"；
   - 单位符号：℃ 改为"摄氏度"，m/s 改为"米每秒"，kg 改为"千克"；
3. 禁止使用任何符号式缩写（如 &、@、#、* 等），全部替换为中文；
4. 表格内容需用纯中文自然语言描述（禁止使用Markdown表格格式），
   例如"表格包含姓名和年龄两列，其中记录了张三的年龄为20岁"；

【输出格式要求】：
严格保留内容的语义完整性，转换后的表述必须准确无误，不要遗漏任何信息。
只输出 Markdown 正文内容，不要添加任何额外说明。
输出的内容请控制行宽，每行不超过80个中文字符，段落之间保留空行，
标题层级清晰，便于读屏软件逐行朗读。
- 如果材料涉及网络安全、攻防、漏洞、利用实验等内容，只做高层次、教学性、非操作性的总结。
- 对安全类内容，只总结学习目标、核心概念、术语、注意事项和防御视角。
- 不要输出利用步骤、payload、shellcode、命令、攻击链或可直接操作的细节。
"""
